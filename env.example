# 数据库配置
DB_HOST=47.102.196.198
DB_PORT=3306
DB_NAME=database
DB_USER=database
DB_PASSWORD=wzm050826

# JWT配置
JWT_SECRET=your-secret-key-change-in-production

# 服务器配置
PORT=5001
NODE_ENV=development

# CORS配置
CORS_ORIGIN=http://localhost:3000

# API配置
API_BASE_URL=http://localhost:5001/api

# 文件上传配置
UPLOAD_MAX_SIZE=10MB
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Redis配置（可选，用于缓存）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# 邮件配置（可选，用于邮箱验证）
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# AI服务配置
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# 安全配置
BCRYPT_ROUNDS=12
SESSION_SECRET=your-session-secret
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# 前端配置
REACT_APP_API_URL=http://localhost:5001/api

# 服务器配置
FRONTEND_URL=http://localhost:3000

# PostgreSQL连接URL (可选，如果设置了会覆盖上面的单独配置)
# POSTGRES_URL=postgresql://username:password@localhost:5432/ai_platform

# Redis配置 (可选，用于会话管理和缓存)
# REDIS_URL=redis://localhost:6379

# 文件上传配置
MAX_FILE_SIZE=10485760  # 10MB
UPLOAD_DIR=uploads

# Redis配置 (可选，用于会话管理和缓存)
# REDIS_URL=redis://localhost:6379

# CORS配置
CORS_ORIGIN=http://localhost:3000 